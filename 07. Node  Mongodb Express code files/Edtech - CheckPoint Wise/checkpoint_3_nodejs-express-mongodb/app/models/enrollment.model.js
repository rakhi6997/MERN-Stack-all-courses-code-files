// when a student enrolls into a course , his credentials are recorded into this database.

// Enrollment schema could something like this :
/*
        mongoose.Schema(
          {
            userId: { type : String , required : true },
            courseId: { type : String , required : true }
          }
  )
*/